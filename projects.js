/* Demo purposes only */
$(".hover").mouseleave(
  function () {
    $(this).removeClass("hover");// This is the javacode that instructs the imae that when you hover over it with the mouse a function will be activated//
  }
);
